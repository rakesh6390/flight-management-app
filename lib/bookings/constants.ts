/** Flat fee applied when a confirmed booking is moved to another flight */
export const RESCHEDULE_FEE_USD = 75;

/** Must cancel or reschedule at least this many hours before departure */
export const BOOKING_CHANGE_MIN_HOURS_BEFORE_DEPARTURE = 2;
